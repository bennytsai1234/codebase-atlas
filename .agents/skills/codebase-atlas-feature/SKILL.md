---
name: codebase-atlas-feature
description: Add Codebase Atlas behavior using the canonical feature workflow.
---

# Codebase Atlas Feature

This is a thin entrypoint. Use the user's current request as task input, then
read and follow `../../../docs/codebase_atlas_feature_workflow.md`.

Start from the atlas index before inspecting code. The canonical workflow owns
the Before / After gate, validation, atlas updates, and delivery policy.
